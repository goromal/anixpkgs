# backup

Rsync-based backup tool.


## Usage

```bash
usage: backup --from SRC --to DST

Mirror SRC to DST using rsync (archive, delete, checksum).

Options:
  --from SRC    Source directory
  --to DST      Destination directory

```

