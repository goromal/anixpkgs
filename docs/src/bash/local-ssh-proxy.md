# local-ssh-proxy

SSH proxy function for registered LAN hosts.


## Usage

```bash
usage: lssh [options] hostname

SSH proxy function for registered LAN hosts.

Options:
  -p              Port

```

